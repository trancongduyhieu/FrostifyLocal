# Handoff: team-plan → team-exec (batch 2, groups A+B)

- **Decided**: Batch 2 = catalog group A (native/JVM desktop, rows 1–11) + group B (packaging/CI, rows 12–24) = **24 skills** (the B header says 12 but carries 13 numbered rows — trust the rows). Owner approved 2026-08-19 right after the batch-1 report. 4 executor workers (opus): worker-6 media-engine/native-binding (rows 1,2,6,7,8), worker-7 platform quirks (3,4,5,9,10,11), worker-8 packaging/config (12–17), worker-9 CI/build infra (18–24). Same output root, house style, and neutral vocabulary as batch 1.
- **New this batch**: every worker prompt carries a **quality bar** distilled from batch-1 verify findings (no stale counts; faithful branch structure + `// adapted` markers; rationale must be the evidence's reason; no project identifiers; quoted grep globs; library-mechanism claims must be re-derivable; expand name abbreviations; OS/version-scope claims). Worker prompts also pre-state the environment quirks (no task-list tools, report to `main`) so nobody burns the discovery detour again.
- **Rejected**: worktree isolation (same reasoning as batch 1 — disjoint new files under an ignored folder); 5th worker (24 splits cleanly into 4 evidence-affine clusters).
- **Risks**: this batch's subject matter (native loading, memory, packaging) sits closest to the restricted vocabulary — the rule is doubled in the prompts; rows sourced from git history (22) and plans (16) have thinner in-repo evidence, so workers must drop-and-report what they cannot confirm.
- **Evidence verified on disk before spawn**: desktopApp/MEMORY_TUNING.md, .omc/plans/conveyor-migration.md, scripts/mpv-linux/, scripts/windows/, .github/workflows/, conveyor.conf (root), composeApp/proguard-desktop-rules.pro.
- **Remaining**: exec 24 files → verify (4 adversarial lanes, batch-1 axes + quality bar) → fix (cap 3) → report. After this batch: groups C, F–N remain (~92 skills); distribution + license decision (GPL question from batch 1) still open with the owner.
