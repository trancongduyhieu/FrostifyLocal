# Handoff: team-exec → team-verify

- **Done**: all 5 workers complete; 29/29 files on disk under `.omc/research/skills-mining/skills/` (82–133 lines each), frontmatter `name` == folder, self-scanned clean for service names / placeholder text / restricted jargon. Worker self-reports are NOT passed to the verify lane (adversarial isolation) — verifiers re-derive everything from the files + evidence.
- **Environment quirk**: subagent sessions have no TaskUpdate tool and no "team-lead" address — workers report to `main` and the lead flips tasks on their behalf. Verify-lane agents will hit the same limits; their prompts must not reference TaskUpdate.
- **Catalog corrections found during exec** (fold into CATALOG.md after verify):
  - Row 37: the "isActive bypass" claim is not what ships — both processors return `true` unconditionally; only a stale KDoc on CrossfadeFilterAudioProcessor still claims the bypass. Skill teaches the real mechanism.
  - Row 38: "cascaded Butterworth" is an imprecise label — two identical Q=0.707 sections give a Linkwitz–Riley-shaped response (−6 dB at cutoff). Skill teaches "verify the response, not the label".
  - Row 45: `getShuffledIndex` was never written; only `getUnshuffledIndex` exists (MediaPlayerInterface.kt:71 + 3 adapters). The skill presents the reverse accessor as the reader's fix without claiming this codebase ships it.
  - Row 58: "enumerate every reachable (from,to) pair" overstates the code — it declares fan-in edges (21/22/23→24) plus direct edges over gaps (no 1→2 edge exists; 1→3 is declared directly). The skill teaches the accurate rule: a chain is only walkable if every intermediate edge exists.
  - Row 57: the arm64 native-slice gap is real but lives in the bundled-driver artifact, not MusicDatabase.kt — confirmed against the cached driver jar (missing windows_arm64 slice); the skill teaches the audit command instead of a fixed slice list.
- **Cross-platform observations from exec (for the owner; not defects in the skill files)**: hand-written migrations and the trigger-recreating onOpen callback appear to be registered only in the android actual (jvm/ios actuals build a bare builder), and the iOS framework links the system sqlite while the DI module installs the bundled driver — lead is verifying before flagging.
- **Two live defects surfaced (lead-verified by grep, flagged to owner as background chips, NOT part of this task)**:
  - `secondLoudnessEnhancer` (androidMain MediaServiceHandlerImpl.kt:203/2293–2294/2394–2396) is used and released but never constructed — the incoming crossfade player runs un-normalized.
  - `currentOrderIndex()` shuffle branch still resolves by id-match (androidMain :2000–2007) with a live caller (ModalBottomSheet.kt:965); the memory note claiming the index bug was fixed by `getShuffledIndex` is being corrected.
- **Vocabulary ruling for the verify lane**: the word "crash" is acceptable — the vocabulary rule's own approved examples use "the crash location"; the ban targets memory-management fault jargon, fault addresses / signal names, and unofficial-API-probing phrasing. Worker-2's flag on worker-1's `guard-on-every-trigger-path` ("crash" ×2) is a false positive; do not fail files for it.
- **Remaining**: worker-5's 2 files → then team-verify (accuracy vs evidence, house style, vocabulary, generality), fix loop cap 3, final report + distribution decision.
