Nutsty Windows Portable Binaries:
----------------------------------
Place optional standalone Windows executables in this directory for zero-setup bundle:
- mpv.exe (and accompanying DLLs)
- ffmpeg.exe & ffprobe.exe
- yt-dlp.exe

When present, Nutsty will automatically prioritize these binaries over system PATH.
